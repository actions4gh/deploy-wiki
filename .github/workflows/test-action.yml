name: Test action
on:
  push:
    branches: "main"
    paths-ignore:
      - wiki/**
      - .gitattributes
      - LICENSE
      - README.md
      - .github/**
      - "!.github/workflows/test-action.yml"
  pull_request:
    paths-ignore:
      - wiki/**
      - .gitattributes
      - LICENSE
      - README.md
      - .github/**
      - "!.github/workflows/test-action.yml"
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  test-action:
    concurrency: wiki-write
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        delete: [true, false]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3
      - uses: ./
        with:
          path: test
          delete: ${{ matrix.delete }}
      - run: git clone https://github.com/${{ github.repository }}.wiki.git
      - run: ls -Ah ${{ github.repository }}.wiki
